apiVersion: v1
kind: Service
metadata:
  name: {{ include "fer-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fer-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fer-service.selectorLabels" . | nindent 4 }}
